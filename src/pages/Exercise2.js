const Exercise2 = () => {
  // Declare a new state variable, which we'll call "count"
  return (
    <div>
      <button onClick={() => alert("You click button1!")}>Button1</button>
      <button onClick={() => alert("You click button2!")}>Button2</button>
      <button onClick={() => alert("You click button3!")}>Button3</button>
    </div>
  );
};

export default Exercise2;
